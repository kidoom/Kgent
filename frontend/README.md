# Frontend V0.1

V0.1 only needs a minimal page later:

- user input
- send button
- final answer
- agent steps showing `tool_use` and `tool_result`

The backend is implemented first so the agent loop is real before UI polish begins.
