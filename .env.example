# Kgent configuration — copy to .env and fill in your values.
# Real environment variables override values in this file.

# OpenAI-compatible API (DeepSeek, OpenAI, etc.)
KGENT_PROVIDER=openai

# Model name (e.g. deepseek-chat, gpt-4.1-mini)
KGENT_MODEL=deepseek-chat

# API key — REQUIRED when KGENT_PROVIDER=openai
KGENT_API_KEY=

# Base URL for OpenAI-compatible APIs (DeepSeek, local proxies, etc.)
KGENT_BASE_URL=https://api.deepseek.com

# Max agent loop iterations (1-32)
KGENT_MAX_STEPS=8

# Max messages kept per session (system prompt + recent tail, 4-500)
KGENT_MAX_SESSION_MESSAGES=100

# Working directory for file tools (default: current directory)
# KGENT_PROJECT_ROOT=.

# Local session persistence (M0.5)
# KGENT_STORAGE_DIR=.kgent
# KGENT_DISABLE_PERSISTENCE=0
# KGENT_TRANSCRIPT_MAX_BYTES=52428800

# FastAPI/ASGI runtime server (frontend client)
# Comma-separated origins; unset = localhost dev defaults (http/https :5173)
# KGENT_CORS_ORIGINS=http://localhost:5173,https://localhost:5173
# Max SSE replay events retained per session (default 500)
# KGENT_SESSION_EVENT_MAX=500

# HTTP server (read by scripts/run_server.py)
# KGENT_HOST=127.0.0.1
# KGENT_PORT=8000
# KGENT_RELOAD=true

# TLS — set both to enable HTTPS via scripts/run_server.py
# KGENT_SSL_KEYFILE=./certs/key.pem
# KGENT_SSL_CERTFILE=./certs/cert.pem

# V0.2 tool permission policy:
#   allow_all   — every tool runs (legacy V0.1 behaviour)
#   risk_based  — low/medium auto-approve, high auto-deny (default)
#   interactive — debug CLI: medium/high require [y/N] approval
KGENT_PERMISSION_MODE=risk_based
